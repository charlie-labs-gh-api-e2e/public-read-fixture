name: Fixture check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fixture:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Verify synthetic fixture
        run: printf 'fixture\n'
